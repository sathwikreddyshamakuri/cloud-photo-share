# AWS
AWS_ACCESS_KEY_ID=CHANGEME                # keep if you use real AWS
AWS_SECRET_ACCESS_KEY=CHANGEME
AWS_DEFAULT_REGION=us-east-1              # ← rename REGION → AWS_DEFAULT_REGION
S3_BUCKET=photo-share-650794551

# JWT / FastAPI
SECRET_KEY=PUT-A-RANDOM-LONG-STRING-HERE   # ← rename JWT_SECRET → SECRET_KEY
TOKEN_EXPIRE_MINUTES=60

# DB (optional)
DATABASE_URL=sqlite:///cloudphoto.db
